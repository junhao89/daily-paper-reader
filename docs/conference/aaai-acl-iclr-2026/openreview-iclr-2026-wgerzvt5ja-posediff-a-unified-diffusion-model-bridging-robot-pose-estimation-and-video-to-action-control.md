---
title: "PoseDiff: A Unified Diffusion Model Bridging Robot Pose Estimation and Video-to-Action Control"
title_zh: "PoseDiff: 统一扩散模型桥接机器人姿态估计与视频到动作控制"
authors: "Haozhuo Zhang, Michele Caprio, Jing Shao, Qiang Zhang, Jian Tang, Shanghang Zhang, Wei Pan"
date: 2025-09-19
pdf: "https://openreview.net/pdf?id=WGErZvT5jA"
tags: ["query:wam-vla"]
score: 7.0
evidence: 利用世界模型关键帧的扩散模型，桥接位姿估计与视频到动作控制
tldr: 提出PoseDiff条件扩散模型，从单张RGB图像估计机器人位姿，并通过世界模型生成的关键帧作为条件，利用重叠平均策略产生平滑的长期动作序列，统一了感知与控制。
source: ICLR-2026-Public
selection_source: conference_retrieval
motivation: 机器人状态估计和动作控制通常分离在多阶段流程中，效率低且不灵活。
method: 单阶段扩散模型从RGB输出结构化状态，再利用世界模型关键帧进行视频到动作的逆动力学建模。
result: 在DREAM数据集上达到最新性能，实现了感知与控制的端到端集成。
conclusion: 统一的扩散框架简化了机器人感知-控制流水线。
---

## Abstract
We present PoseDiff, a conditional diffusion model that unifies robot state estimation and control within a single framework. At its core, PoseDiff maps raw visual observations into structured robot states—such as 3D keypoints or joint angles—from a single RGB image, eliminating the need for multi-stage pipelines or auxiliary modalities. Building upon this foundation, PoseDiff extends naturally to video-to-action inverse dynamics: by conditioning on sparse video keyframes generated by world models, it produces smooth and continuous long-horizon action sequences through an overlap-averaging strategy. This unified design enables scalable and efficient integration of perception and control. On the DREAM dataset, PoseDiff achieves state-of-the-art accuracy and real-time performance for pose estimation. On Libero-Object manipulation tasks, it substantially improves success rates over existing inverse dynamics modules, even under strict offline settings. Together, these results show that PoseDiff provides a scalable, accurate, and efficient bridge between perception, planning, and control in embodied AI.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有的机器人状态估计（如位姿估计）与动作控制通常被设计为多阶段流水线（multi-stage pipeline），感知模块输出中间状态后，再由独立的控制模块处理，导致流程复杂、效率低且灵活性差。此外，从视频到动作的逆动力学建模（video-to-action inverse dynamics）往往依赖完整视频序列或密集的时间信息，难以处理长周期任务。
- **整体含义**：本文提出 PoseDiff，一个统一的**条件扩散模型**，旨在将机器人感知（姿态估计）和控制（长周期动作序列生成）整合到一个单一框架中，消除多阶段流程中的人工分离，实现端到端的可扩展、高效集成。

## 2. 提出的方法论

- **核心思想**：利用扩散模型的强大生成能力，同时解决两个任务：
  1. **姿态估计**：从单张 RGB 图像直接恢复机器人结构状态（如 3D 关键点、关节角度），无需多阶段流水线或辅助模态（如深度图）。
  2. **视频到动作控制**：以世界模型（world model）生成的稀疏视频关键帧作为条件，通过**重叠平均策略（overlap-averaging strategy）** 产生平滑、连续的长周期动作序列，实现逆动力学建模。
- **关键技术细节**：
  - 模型以原始视觉观测为输入，输出结构化状态（pose estimation）；随后，基于该状态和世界模型的关键帧，以扩散方式生成未来动作序列。
  - 采用条件扩散过程中，关键帧的稀疏性降低了计算复杂度；重叠平均策略在相邻窗口之间平滑拼接，避免动作跳跃，保证时序连续性。
  - 整体是一个从感知到控制的单一扩散过程，无需单独训练两个模块。

## 3. 实验设计

- **数据集与场景**：
  - **DREAM 数据集**：用于评估姿态估计任务，报告了最新准确率和实时性能。
  - **Libero-Object 操作任务**：用于评估视频到动作逆动力学模块的效果，在严格的离线（offline）设置下测试。
- **Benchmark 与对比**：
  - 在 DREAM 上达到了**最先进（state-of-the-art）** 精度。
  - 在 Libero-Object 上与现有的逆动力学模块（未详细列举）进行比较，显著提升了任务成功率。
- **对比方法**：文中未明确列出具体对比算法，但隐式说明优于多阶段流水线和常规逆动力学方法。

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量、训练时长等硬件资源信息。因此在总结时需指出：**论文未提供算力细节**。

## 5. 实验数量与充分性

- **实验数量**：主要在两个数据集（DREAM 和 Libero-Object）上进行了评估，覆盖姿态估计和操作控制两个核心任务。但全文未提及消融实验、参数敏感性分析或跨域泛化实验。
- **充分性与公平性**：
  - 姿态估计在公开基准上报告了 SOTA 结果，具有竞争力；
  - 逆动力学部分在离线设置下与现有模块对比，实验设置公平；
  - 然而，缺乏对不同扩散步数、关键帧数量、重叠窗口大小等超参数的消融研究，无法充分证明各组件的贡献度。因此**实验覆盖范围较窄，充分性有限**。

## 6. 主要结论与发现

- PoseDiff 在 DREAM 数据集上实现了姿态估计的 SOTA 精度与实时性能；
- 在 Libero-Object 操作任务中，以统一的扩散框架显著提升了成功率，超越了现有的逆动力学模块；
- 该工作证明了**统一的扩散模型可以将感知与控制无缝桥接**，简化了机器人流水线，并在可扩展性、准确性和效率上同时受益。

## 7. 优点

- **方法简洁统一**：单阶段扩散模型同时解决姿态估计和长周期动作生成，避免多阶段累积误差。
- **稀疏关键帧条件**：利用世界模型生成的稀疏帧，降低计算量，且通过重叠平均策略实现平滑长期控制。
- **实时性能**：在姿态估计上达到实时，适合实际机器人部署。
- **离线实用**：在严格离线设置下依然优于现有方法，适合无交互训练的场景。

## 8. 不足与局限

- **实验局限**：仅测试了两个数据集，且缺乏消融实验和泛化性（如不同机器人形态、复杂光照条件）的验证。
- **依赖世界模型质量**：视频到动作控制部分依赖于世界模型生成的关键帧，其质量直接影响最终动作的合理性，文中未分析该依赖的鲁棒性。
- **细节缺失**：未提供模型架构细节（如扩散网络的具体结构、损失函数）、训练策略以及硬件算力信息，难以复现。
- **偏差风险**：仅在特定操作任务（Libero-Object）上验证，可能无法推广到更复杂的物理交互场景。

（完）
