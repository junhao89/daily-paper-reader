---
title: "PoseDiff: A Unified Diffusion Model Bridging Robot Pose Estimation and Video-to-Action Control"
title_zh: PoseDiff：统一机器人姿态估计与视频到动作控制的扩散模型
authors: "Haozhuo Zhang, Michele Caprio, Jing Shao, Qiang Zhang, Jian Tang, Shanghang Zhang, Wei Pan"
date: 2025-09-19
pdf: "https://openreview.net/pdf?id=WGErZvT5jA"
tags: ["query:wam-vla"]
score: 9.0
evidence: 使用世界模型关键帧统一机器人状态估计和视频到动作控制的扩散模型
tldr: 针对机器人感知与控制分离的问题，PoseDiff提出一种条件扩散模型，从单张RGB图像直接估计机器人状态（如3D关键点或关节角），并利用世界模型生成的关键帧通过重叠平均策略产生平滑的长时程动作序列。在DREAM数据集上达到最优性能，展示了感知与控制的统一框架的有效性。
source: ICLR-2026-Public
selection_source: conference_retrieval
motivation: 现有机器人系统通常将感知与控制分离，导致流水线复杂且效率低。
method: 提出条件扩散模型PoseDiff，从RGB图像映射到结构化状态，并利用世界模型关键帧进行视频到动作的逆动力学预测。
result: 在DREAM数据集上取得最优结果，实现平滑且连续的长时程动作序列。
conclusion: 统一的扩散框架能够高效集成感知与控制，具有可扩展性。
---

## Abstract
We present PoseDiff, a conditional diffusion model that unifies robot state estimation and control within a single framework. At its core, PoseDiff maps raw visual observations into structured robot states—such as 3D keypoints or joint angles—from a single RGB image, eliminating the need for multi-stage pipelines or auxiliary modalities. Building upon this foundation, PoseDiff extends naturally to video-to-action inverse dynamics: by conditioning on sparse video keyframes generated by world models, it produces smooth and continuous long-horizon action sequences through an overlap-averaging strategy. This unified design enables scalable and efficient integration of perception and control. On the DREAM dataset, PoseDiff achieves state-of-the-art accuracy and real-time performance for pose estimation. On Libero-Object manipulation tasks, it substantially improves success rates over existing inverse dynamics modules, even under strict offline settings. Together, these results show that PoseDiff provides a scalable, accurate, and efficient bridge between perception, planning, and control in embodied AI.

---

## 论文详细总结（自动生成）

# PoseDiff：统一机器人姿态估计与视频到动作控制的扩散模型

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有机器人系统通常将感知（状态估计）与控制（动作生成）分离为独立模块，导致流水线复杂、效率低下，且依赖多阶段处理或辅助传感器（如深度摄像头、IMU）。
- **整体含义**：PoseDiff 提出一个**单一统一框架**，将机器人状态估计和基于视频的动作控制整合到一个条件扩散模型中，实现从原始视觉观测到结构化状态（3D关键点或关节角）的直接映射，并进一步扩展到视频到动作的逆动力学预测，旨在简化感知-规划-控制链路，提升可扩展性和效率。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：使用条件扩散模型（conditional diffusion model）对机器人状态进行概率建模，从单张 RGB 图像直接推断结构化状态，并利用世界模型生成的关键帧通过重叠平均策略生成平滑的长时程动作序列。
- **关键技术细节**：
  - **状态估计分支**：将 RGB 图像作为条件输入扩散模型，输出机器人 3D 关键点或关节角，无需多阶段流水线或其他模态数据。
  - **视频到动作逆动力学**：将世界模型生成的稀疏视频关键帧作为条件，引导扩散模型生成连续动作序列。通过**重叠平均（overlap-averaging）策略**平滑相邻片段间的动作，保证长时程动作的连续性。
  - **统一架构**：状态估计和动作生成共用同一扩散模型框架，仅需调整条件编码方式，无需重新设计网络结构。

## 3. 实验设计

- **数据集与场景**：
  - **DREAM 数据集**：用于机器人姿态估计任务，评估 3D 关键点和关节角预测的准确性与实时性。
  - **Libero-Object 操作任务**：用于视频到动作控制任务，测试逆动力学模块在严格离线设置下的成功率。
- **Benchmark**：在 DREAM 上对比现有姿态估计方法（未在元数据中列出具体对比方法，但声称达到 state-of-the-art）；在 Libero-Object 上对比现有逆动力学模块。
- **对比方法**：原摘要提到“显著提升现有逆动力学模块的成功率”，但未给出详细名称。

## 4. 资源与算力

- **说明**：论文 PDF 提取内容中**未提及**使用的 GPU 型号、数量、训练时长等算力信息。元数据中也没有相关描述。因此无法总结具体的算力消耗。

## 5. 实验数量与充分性

- **实验分组**：至少包含两大组实验——姿态估计（DREAM）和视频到动作控制（Libero-Object）。可能还包含消融实验（如对重叠平均策略、条件扩散模型内部组件的分析），但元数据未明确列出。
- **充分性与客观性**：
  - 实验覆盖了感知（姿态估计）和控制（动作生成）两个核心任务，且使用公开数据集（DREAM、Libero-Object），具有可比性。
  - 在离线设置下评估，符合真实机器人训练场景，避免了在线干预的影响，结果客观。
  - 不足之处在于未提供与现有方法详细的数值对比表格（由于元数据有限），也未讨论泛化到其他机器人平台或场景的测试。

## 6. 主要结论与发现

- 在 DREAM 数据集上达到**最先进的姿态估计精度**，同时保持实时性能。
- 在 Libero-Object 操作任务上，PoseDiff 作为逆动力学模块相比现有方法**显著提高了成功率**（尤其在没有在线交互的严格离线环境下）。
- 统一的扩散框架能够**高效集成感知与控制**，且具有可扩展性（可扩展到不同状态表示和动作空间）。

## 7. 优点

- **框架统一性**：首次将状态估计和逆动力学控制融合进同一扩散模型，简化了传统多模块流水线。
- **数据高效**：仅需单张 RGB 图像即可估计状态，无需额外传感器或多视角输入。
- **生成平滑性**：通过重叠平均策略有效抑制长时程动作的不连续性，适合实际机器人执行。
- **离线适用**：在严格离线设置下仍能获得高成功率，降低了在线交互的成本和风险。

## 8. 不足与局限

- **实验覆盖有限**：仅验证了两个数据集，缺乏对其他机器人形态（如人形机器人、移动机械臂）或更复杂动态场景的测试。
- **对比不充分**：未在元数据中列出具体基线方法的性能数值，也未进行跨动作空间（速度级 vs 位置级）的对比。
- **资源消耗未知**：未报告训练或推理的算力需求，难以评估其在边缘设备上的部署可行性。
- **世界模型依赖**：视频到动作控制部分依赖外部世界模型生成关键帧，世界模型的误差可能传播到动作生成中。
- **状态表示受限**：仅测试了 3D 关键点和关节角两种表示，对其他结构化状态（如末端位姿、物体相对 pose）的适用性未知。

（完）
