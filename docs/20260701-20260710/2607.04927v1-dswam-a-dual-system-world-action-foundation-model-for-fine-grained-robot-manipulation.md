---
title: "DSWAM: A Dual-System World Action Foundation Model for Fine-Grained Robot Manipulation"
title_zh: DSWAM：一种用于精细机器人操作的双系统世界动作基础模型
authors: "Jian Zhu, Jianjun Zhang, Taiyi Su, Tianbin Liu, Zhangyuan Wang, Kai Xie, Zitai Huang, Chong Ma, Youzhang He, Tianjian Wang, Hanyang Wang, Weihao Ding, Yi Xu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04927v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 双系统世界动作模型
tldr: 机器人操作领域，现有世界动作模型（WAM）擅长物理执行但缺乏语言级任务分解，而VLM政策能分解指令但执行不够精细。本文提出DSWAM双系统模型，保留WAM作为默认控制路径，并可选激活视觉语言子任务规划器。在DeMaVLA变形操作设置下，通过TensorRT加速和实时分块实现高效推理。实验证明DSWAM在细粒度操作任务中优于纯VLA和纯WAM方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04927v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 887, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04927v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1583, \"height\": 2126, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1656, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1209, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1215, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.04927v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1290, \"height\": 280, \"label\": \"Table\"}]"
motivation: 现有WAM缺乏语言级任务分解接口，而VLA在执行时不够精细，且两者缺乏公平的真实机器人对比。
method: DSWAM包含System 1 WAM执行器和System 2 VLM子任务规划器，通过动作预测与视频联合训练，集成TensorRT加速和实时分块。
result: 在DeMaVLA设置下，DSWAM在细粒度操作任务中实现了更高的成功率和效率，优于纯VLA和纯WAM基线。
conclusion: DSWAM通过双系统协同，填补了WAM与VLA之间的能力鸿沟，为精细机器人操作提供了有效且实用的方案。
---

## 摘要
世界动作模型（WAMs）通过基于视频的世界建模作为机器人动作学习的密集监督，为视觉-语言-动作（VLA）策略提供了一种有前景的替代方案。现有的WAMs擅长基于物理的执行，但通常缺乏基于VLM的VLA中用于分解粗略指令的显式语言级规划接口。当家务任务涉及复杂的多步目标时，这种分解变得重要，因为粗略的用户命令需要转换为精细可执行子任务序列。同时，该领域仍然缺乏VLA和WAM执行能力之间的公平真实机器人比较，因为现有系统在数据、机器人实体和任务协议上往往不同。为了解决分解差距以及需要受控的WAM-VLA比较，我们引入了DSWAM，一种用于精细机器人操作的双系统世界动作基础模型。DSWAM保持系统1的WAM执行器作为默认控制路径，并仅在任务分解有用时可选地激活系统2的视觉-语言子任务规划器。规划器从短期视觉历史和全局任务提示中预测可执行的子任务，而WAM执行器为每条指令或子任务执行具有世界感知的动作生成。执行器通过动作预测和视频联合训练进行训练，但推理时直接预测动作块，无需显式的未来视频生成。为了使该执行路径在真实机器人上实用，我们进一步集成了TensorRT加速、异步执行和实时分块（RTC），使得策略查询不会阻塞机器人控制。为了与VLA策略进行公平的真实机器人比较，我们在DeMaVLA真实世界可变形操作设置下构建和评估DSWAM，匹配机器人平台、预训练数据、后训练数据和评估标准。

## Abstract
World Action Models (WAMs) provide a promising alternative to Vision-Language-Action (VLA) policies by using video-based world modeling as dense supervision for robot action learning. Existing WAMs excel at physically grounded execution, but typically lack the explicit language-level planning interface in VLM-based VLAs for decomposing coarse instructions. Such decomposition becomes important when household tasks involve complex multi-step goals, where coarse user commands need to be converted into sequences of fine-grained executable subtasks. Meanwhile, the field still lacks a fair real-robot comparison between VLA and WAM execution capabilities, since existing systems often differ in data, robot embodiments, and task protocols. To address both the decomposition gap and the need for a controlled WAM-VLA comparison, we introduce DSWAM, a Dual-System World Action Foundation Model for fine-grained robot manipulation. DSWAM keeps a System 1 WAM executor as the default control path and optionally activates a System 2 vision-language subtask planner only when task decomposition is useful. The planner predicts executable subtasks from short-term visual history and a global task prompt, while the WAM executor performs world-aware action generation for each instruction or subtask. The executor is trained with action prediction and video co-training, but inference directly predicts action chunks without explicit future video generation. To make this execution path practical on real robots, we further integrate TensorRT acceleration, asynchronous execution, and real-time chunking (RTC) so that policy queries do not block robot control. To provide a fair real-robot comparison with VLA policies, we build and evaluate DSWAM under the DeMaVLA real-world deformable manipulation setting with matched robot platform, pretraining data, post-training data, and evaluation criteria.

---

## 论文详细总结（自动生成）

# DSWAM 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有世界动作模型（WAM）擅长基于视频世界建模的精细物理执行（如接触丰富的操作），但缺乏语言级的任务分解接口，无法将粗粒度用户指令（如“整理桌子”）自动拆分为可执行的子任务序列。而基于VLM的VLA策略能分解指令，但在低层动作执行上不够精细。同时，WAM与VLA之间缺乏公平的真实机器人对比，因为现有系统在数据、机器人本体、任务协议上差异很大。
- **整体含义**：DSWAM旨在弥合WAM与VLA之间的能力鸿沟，通过双系统设计将语义任务分解与物理执行解耦，并在匹配条件下提供WAM与VLA的公平对比，证明WAM在精细操作上的优势。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 核心思想
- **双系统架构**：
  - **System 1（默认控制路径）**：WAM执行器，直接根据当前观测、语言指令和本体感知预测动作块，具备世界意识。
  - **System 2（可选模块）**：基于视觉-语言模型的子任务规划器，仅在需要时将粗指令分解为有序可执行子任务。
- **训练与推理分离**：执行器训练时采用动作预测和视频联合训练（视频共训练提供世界建模监督），但推理时直接输出动作块，不生成未来视频帧，从而降低延迟。

### 关键技术细节
1. **多模态观测**：  
   \( O_t = [I_t^1, I_t^2, I_t^3, \ell_t, q_t] \)  
   其中 \( I \) 为多视角RGB图像，\( \ell_t \) 为语言指令或子任务，\( q_t \) 为机器人状态。

2. **System 2 规划器**：  
   - 输入：短期视觉历史 \( X_t \)（最近5帧，1 Hz采样）和全局任务提示 \( p \)。  
   - 输出：下一子任务指令 \( s_t = P_\phi(X_t, p) \)。  
   - 边界监督：训练时使用滑动窗口和过渡感知标签（当前子任务、下一子任务、结束标记）。  
   - 优化目标：自回归语言建模损失 \( L_{\text{plan}} \)。

3. **System 1 执行器**：  
   - 编码当前上下文得到潜在世界特征 \( z_t = f_\theta(\cdot) \)，再预测动作块 \( A_t = [a_t, \dots, a_{t+H-1}] \)。  
   - 采用条件流匹配（conditional flow matching）生成连续动作块。  
   - 联合训练损失：\( L = L_{\text{act}} + \lambda_{\text{vid}} L_{\text{vid}} \)，其中 \( L_{\text{act}} = L_{\text{FM}}(A_t) \)，\( L_{\text{vid}} = L_{\text{FM}}(V_{t+1:t+T}) \)。  
   - 训练时未来视觉token提供监督，但动作token不使用未来信息，确保推理时一致性。

4. **高效部署机制**：  
   - **TensorRT加速**：将视觉上下文引擎和动作去噪引擎分别编译为两个TensorRT引擎，BF16精度下延迟从198.2ms降至73.8ms（2.69倍加速）。  
   - **实时分块（RTC）**：异步执行，策略查询不阻塞机器人控制，同时执行当前块并预测下一块。

5. **System 1与System 2耦合**：启用System 2时，每2秒发送5帧给规划器，返回子任务指令给执行器。

## 3. 实验设计：数据集、基准与对比方法

### 数据集与场景
1. **RoboTwin 2.0 仿真基准**：50个双臂操作任务，分为清洁设置（固定初始配置）和随机设置（物体位姿和场景布局变化）。
2. **DeMaVLA 真实世界折叠协议**：四种衣物（衬衫、裙子、裤子、毛巾）的折叠任务，每种两个实例（容易和困难），每个实例10次试验，共20次/类。任务全过程包括从篮子中取衣、放置、铺平、折叠。
3. **System 2 排序任务**：真实桌面整理任务，粗指令“整理桌子上的物品”，分解为“拿起玩具车放入盒子”和“拿起面包放在盘子”两个子任务。

### 对比方法
- **RoboTwin 2.0**：与π0、π0.5、DeMaVLA、Motus、Fast-WAM比较。
- **真实世界折叠**：与π0、DeMaVLA比较（同一平台、数据、任务、评估标准）。
- **System 2研究**：对比原始粗指令训练与子任务指令训练的执行稳定性和成功率。

### 评估指标
- 成功率（SR）
- 平均完成时间（Completion Time）
- 执行错误次数/ rollout

## 4. 资源与算力

- 论文中未明确说明训练所用的GPU型号、数量及训练时长。
- 提到推理加速时使用了**NVIDIA GeForce RTX 5090 + CUDA 12.9 + TensorRT 10.16.1**进行TensorRT编译和推理。
- 训练数据：5,000+小时的真实世界双人形臂操作数据（未详述训练硬件配置）。

## 5. 实验数量与充分性

### 实验数量
- **RoboTwin 2.0**：50个任务×2种设置（清洁/随机），每个方法均报告平均成功率。
- **真实世界折叠**：4个类别×2个实例×10次试验 = 80次试验，报告成功率与完成时间。
- **System 2研究**：两种训练步数（6000 / 18000），各报告成功率和错误次数。
- **效率评估**：TensorRT延迟测试（6个种子）、真实机器人rollout（衬衫和裤子各10次，同步vs异步）。

### 充分性与公平性
- **优点**：真实世界折叠基准完全匹配DeMaVLA条件（机器人平台、训练数据、任务协议、成功标准），确保WAM与VLA对比公平。RoboTwin 2.0覆盖广泛双臂操作技能。System 2实验隔离了分解效果。
- **不足**：仅覆盖一种机器人本体（ALOHA风格双机械臂），未测试其他本体。场景局限于桌面折叠和简单排序，未涉及移动操作或复杂家庭环境。未报告训练资源消耗，可复现性受限。

## 6. 论文的主要结论与发现

1. **WAM执行器优于VLA**：在匹配条件下，DSWAM（WAM-only模式）将真实世界折叠成功率从92.5%（DeMaVLA）提升至96.3%，完成时间从2分18秒缩短至1分44秒。
2. **仿真通用性**：在RoboTwin 2.0上，DSWAM在清洁和随机设置下分别达到92.38%和91.90%平均成功率，优于所有对比方法（包括VLA和WAM基线）。
3. **System 2有效性**：对于需要分解的粗指令，子任务级监督将成功率从约75.7%提升至100%，错误次数从3.53降至0.65。
4. **高效部署可行**：TensorRT + RTC实现73.8ms推理延迟，异步执行保持100%成功率同时加速。

## 7. 优点

- **双系统解耦设计**：灵活启用/禁用高层规划，避免过度规划，同时保持物理执行优势。
- **公平对比**：首次在匹配真实机器人条件下系统比较WAM与VLA，减少混淆因素。
- **训练-推理分离**：利用视频共训练获得世界知识，但推理无视频生成，兼顾性能与效率。
- **实用性**：集成TensorRT加速和RTC，延迟仅73.8ms，适合实时控制。
- **广泛评估**：覆盖仿真和真实环境，多种任务类型（刚体、可变形物体）。

## 8. 不足与局限

- **本体单一**：仅评估ALOHA风格双臂平台，未在其它机器人本体（如单臂、灵巧手）上验证。
- **任务范围有限**：真实实验限于折叠与简单排序，未涉及复杂场景、移动操作或人机交互。
- **资源信息缺失**：未报告训练GPU型号、数量、时长，难以评估计算成本与复现代价。
- **System 2规划粒度**：子任务由人为标注，未实现端到端自动分解学习。
- **统计分析不足**：未提供置信区间或显著性检验，结果可能存在随机波动。
- **潜在偏差**：数据集中可能包含与评估任务高度相似的样本，泛化性需进一步验证。

（完）
