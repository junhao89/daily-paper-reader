---
title: "Anticipate Before Acting: Future-State-Conditioned Vision-Language Navigation"
title_zh: 先预测后行动：未来状态条件化的视觉语言导航
authors: "Lingfeng Zhang, Zhanguang Zhang, Liheng Ma, Tongtong Cao, Yingxue Zhang"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18042v2"
tags: ["query:latent-vla"]
score: 8.0
evidence: 未来状态条件视觉语言导航，联合嵌入预测方法
tldr: 针对端到端视觉语言导航中标准行为克隆只监督下一动作、缺乏对未来视觉结果预测能力的问题，提出未来状态条件化的导航模型FSC-VLN。该方法在因果策略中加入未来查询token，训练时与未来视觉嵌入对齐以蒸馏未来信息，推理时仅需当前观测。在R2R验证集上，FSC-VLN在两种训练数据设置下均提升了SR、OSR和SPL指标，尤其在长距离任务中增益显著，且仅增加两个前缀token，计算开销极小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18042v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1429, \"height\": 685, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18042v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1361, \"height\": 410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 361, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18042v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1291, \"height\": 248, \"label\": \"Table\"}]"
motivation: 标准行为克隆只监督下一动作而不鼓励策略状态预测未来视觉结果，限制了长程决策能力。
method: 在因果策略中加入未来查询token，训练时将其与未来视觉嵌入对齐，推理时仅需当前观测。
result: R2R val-unseen上SR/OSR/SPL均提升，长程任务增益更大。
conclusion: 轻量级未来状态蒸馏方法有效提升导航性能，且推理成本极低。
---

## 摘要
端到端的视觉语言导航（VLN）使用因果视觉语言模型将指令和第一人称观测直接映射到动作，但标准的行为克隆仅监督下一个动作，并未明确鼓励策略状态预测未来的视觉结果，从而限制了长程决策。特权输入诊断显示，访问专家轨迹的未来图像可以显著改善导航，表明未来观测包含丰富、可操作的线索，尽管此类输入在部署时不可用。受此信号启发，我们提出了未来状态条件化视觉语言导航（FSC-VLN），这是一种可部署的模型，通过增加一个未来查询令牌来增强因果策略，并使用仅训练时的未来状态监督，将未来观测的信息提炼到策略状态中。具体来说，在训练期间，我们将未来查询表示与冻结的视觉嵌入对齐（未来Δ步），而推理仅需过去和当前观测。这种设计保留了基线推理模式，仅增加了两个学习到的前缀令牌，意味着最小的额外开销。在R2R验证集未见过的场景上，FSC-VLN在两种训练数据机制下均优于StreamVLN风格的基线，在长程任务中获得更大提升；消融实验进一步支持了将未来查询和动作查询分离的双查询设计。

## Abstract
End-to-end vision-language navigation (VLN) with causal vision-language models maps instructions and egocentric observations directly to actions, but standard behavior cloning supervises only the next action and does not explicitly encourage the policy state to be predictive of future visual outcomes, limiting long-horizon decision making. A privileged-input diagnostic shows that access to an expert-trajectory future image can substantially improve navigation, indicating that future observations contain rich, actionable cues, though such inputs are unavailable at deployment. Motivated by this signal, we propose Future-State-Conditioned VLN (FSC-VLN), a deployable model that augments a causal policy with a future-query token and uses training-only future-state supervision to distill information from future observations into the policy state. Concretely, during training we align the future-query representation to a frozen visual embedding $Δ$ steps ahead, while inference requires only past and current observations. This design preserves the baseline inference pattern and adds only two learned prefix tokens, implying minimal overhead. On R2R val-unseen, FSC-VLN improves SR/OSR/SPL over a StreamVLN-style baseline under two training-data regimes, with larger gains on long-horizon episodes; ablations further support the dual-query design that separates future and action queries.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：端到端视觉语言导航（VLN）中，标准的行为克隆（Behavior Cloning）只监督下一动作预测，未显式鼓励策略状态预测未来视觉结果，导致长程决策能力受限。微小误差在长时间执行过程中不断累积，难以纠正。
- **动机与意义**：通过特权输入诊断（在训练和测试时给模型提供专家轨迹的未来图像）发现未来观测包含丰富的导航线索（SR提升41.81个百分点），证明了未来信息对导航的潜在价值。但该诊断不可部署（推理时无法获得未来图像）。因此作者提出一种可部署的方法——FSC-VLN，在训练时通过未来状态监督将未来视觉信息蒸馏到策略状态中，推理时仅需当前和历史观测，不改变推理模式。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 在因果策略中插入两个可学习的前缀令牌：**未来查询（qF）** 和**动作查询（qA）**，放置在上下文令牌之后、动作令牌之前。训练时，qF的隐藏状态被对齐到未来Δ步观测的冻结视觉嵌入（来自共享视觉编码器，经停止梯度处理），从而将未来视觉信息蒸馏到策略状态中。动作查询可以因果地关注未来查询，用于生成动作。

### 关键技术细节
- **模型架构**：基于StreamVLN基线，使用Qwen2-7B作为因果语言模型，SigLIP-SO400M作为冻结视觉编码器，视觉投影器（两层MLP+GELU）映射到语言模型维度。
- **输入序列**：对于每个观测步骤，序列为 `Ct, qF, qA, at`（C_t为上下文令牌，包含指令、历史观测和当前观测的视觉令牌），因果Transformer处理。
- **未来目标构建**：对专家轨迹中t+Δ步的观测，经共享冻结视觉编码器和投影器得到网格特征，再进行平均池化得到目标特征z*_{t+Δ}，施加停止梯度。
- **未来状态对齐损失**：L_future = || g(ĥF_t) - g(z*_{t+Δ}) ||₁，其中g为投影函数，将高维特征降至低维。损失以ℓ1范数计算。总损失 L_total = L_LM + λF * L_future，λF=0.1。
- **推理**：丢弃未来目标分支，仅进行单次因果预填充（qF和qA作为前缀），然后自回归解码动作令牌。不生成未来潜变量，无额外模型调用。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：标准室内VLN基准：R2R（Room-to-Room）和RxR（Room-across-Room），在R2R val-unseen（1839个片段）上报告主要结果。训练数据采用两种规模：
  - 小规模：仅R2R+RxR
  - 大规模：R2R+RxR+EnvDrop（数据增强）+ScaleVLN（额外大规模训练集）
- **基准（Baseline）**：StreamVLN风格的端到端VLM导航策略。所有共享组件（骨干网络、优化器、学习率调度、批大小、数据预处理、解码设置）与StreamVLN一致，保证公平比较。
- **对比方法**：主要对比基线（StreamVLN）以及未使用未来监督的FSC-VLN变体（仅未来查询、无动作查询）。未与其他VLN方法（如NaVid、Uni-NaVid等）进行直接横向比较，因为论文聚焦于在特定基线上的改进验证。

## 4. 资源与算力
- **GPU**：4台GPU，每设备批大小为1，梯度累积16步（全局批大小64）。
- **训练配置**：使用bfloat16精度、梯度检查点、DeepSpeed ZeRO-2。优化器为AdamW，学习率2e-5，无权重衰减，预热比例0.075，余弦调度，梯度裁剪1.0。
- **训练时长**：论文未明确说明具体训练时长（如小时数），仅提及训练一个epoch，每个样本为最多32个低级控制步的轨迹片段。

## 5. 实验数量与充分性
- **实验组数**：
  - 特权输入诊断实验（表1）：1组对比，证明未来图像的价值。
  - 主要结果（表2）：2种训练数据规模下的FSC-VLN vs 基线。
  - 长短轨迹分层分析（表3）：分别报告短程（≤10m）和长程（>10m）的指标。
  - 消融实验（表4）：比较双查询（qF+qA）与单查询（仅qF）的性能差异，并进一步按长/短程分层。
  - 共约6组主要实验表格。
- **充分性评价**：实验设计较为合理，覆盖了不同训练数据规模、轨迹长度分层、关键消融。但横向比较缺乏：未与其他最新VLM导航方法（如NaVid、Uni-NaVid、NaVILA）对比绝对性能，仅在基线上证明相对提升。此外仅采用R2R val-unseen一个测试集，未在RxR或真实环境等更多场景验证泛化性。总体而言，在提出的改进层面实验充分，但在广度上有所局限。

## 6. 论文的主要结论与发现
- 特权输入诊断证实未来观测包含可操作的导航线索。
- FSC-VLN在两种训练数据规模下均提升SR、OSR、SPL，且改进主要集中在长程轨迹（>10m）上（长程SR提升最高达3.85个百分点），短程影响很小。
- 双查询设计（未来查询+动作查询）优于单查询（仅未来查询），分离未来预测与动作解码有助于将未来监督信号更有效地转化为导航行为（SR下降0.6个百分点，SPL下降1.05个百分点）。
- 方法保持基线推理模式，仅增加两个前缀令牌，推理成本几乎不变。

## 7. 优点：方法或实验设计上的亮点
- **高效性**：轻量级设计，仅增加两个可学习令牌，训练时使用未来监督但推理时完全无额外开销，便于部署。
- **诊断实验有说服力**：通过特权输入诊断直接验证未来信息的价值，为后续方法设计提供强有力动机。
- **分层分析**：区分短/长程轨迹，揭示了未来监督对长程导航的关键作用，使结论更有针对性。
- **消融设计清晰**：通过对比双查询与单查询，证明了动作查询的必要性，解释了未来监督提升的机制。
- **公平比较**：在相同骨干、优化设置、数据预处理下与基线对比，确保改进归因于提出的方法。

## 8. 不足与局限
- **缺乏与最新方法的横向对比**：未与NaVid、Uni-NaVid、NaVILA等当前SOTA方法在同一条件下比较，因此绝对性能提升的相对价值不清晰。
- **实验场景单一**：仅在R2R val-unseen上报告主要结果，未在RxR、更多未见环境或真实机器人平台上验证泛化性。
- **未来目标基于专家轨迹**：专家未来帧与策略on-policy的未来可能不一致，误差累积可能削弱监督信号的有效性。
- **固定预测距离Δ**：使用固定的Δ=32步，可能不适合所有场景和轨迹长度，缺乏自适应策略。
- **单骨干验证**：仅使用Qwen2-7B+SigLIP，未在其他VLM骨干（如LLaMA、ViT-GPT等）上验证方法通用性。
- **计算资源描述不完整**：未说明训练总时间或能耗，难以评估可复现性。

（完）
