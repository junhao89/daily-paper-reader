---
title: "The SIGReg Objective as Variational Free Energy: A Theoretical Active-Inference Account of JEPA World Models"
title_zh: SIGReg目标作为变分自由能：JEPA世界模型的理论主动推断解释
authors: "Fabio Arnez, Alexandra Gomez-Villa"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13612v1"
tags: ["query:latent-vla"]
score: 8.0
evidence: 对联合嵌入预测架构（JEPA）用于潜在世界模型的理论分析
tldr: JEPA世界模型通常凭经验设计，缺乏规范性原理。本文从Active Inference视角分析，证明反坍塌正则化器的选择决定JEPA目标是否对应变分自由能。通过层级分析发现SIGReg消除熵估计间隙，使目标成为精确信息瓶颈，而VICReg等存在不可约各向异性项。这为JEPA提供了理论解释，并指出当前模型缺失状态认知价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13612v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1379, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13612v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 999, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13612v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 979, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13612v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1415, \"height\": 360, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13612v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1460, \"height\": 605, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13612v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1380, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13612v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1366, \"height\": 465, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13612v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1454, \"height\": 1232, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13612v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1460, \"height\": 566, \"label\": \"Table\"}]"
motivation: JEPA世界模型缺乏规范性原理，仅凭经验设计，需要从第一性原理统一解释其训练目标。
method: 从Active Inference变分自由能视角，组织四个非对比正则化器（VICReg、LogDet、PairDist、SIGReg）成熵估计层级，分析间隙符号的影响。
result: SIGReg消除间隙，使JEPA目标成为精确信息瓶颈，保留惊奇边界；VICReg留下不可约各向异性项。理论与多步期望自由能等扩展一致。
conclusion: SIGReg为JEPA提供了规范的Active Inference基础，且状态认知价值是当前模型未计算的信号。
---

## 摘要
联合嵌入预测架构（JEPAs）是潜在世界模型的主导设计，但它们通常通过经验性能而非规范原则来证明其合理性。我们证明，防坍塌正则化器的选择决定了JEPA的训练目标（一个预测损失加上一个加权嵌入正则化器）是否构成有效的主动推断（AIF）变分自由能。我们将四种非对比正则化器（VICReg、LogDet、PairDist和SIGReg）组织成一个由先验校准误差间隙索引的熵估计器层次结构，并表明该间隙的符号（即估计器是从上界还是下界约束潜在熵）决定了AIF惊奇界是否成立：VICReg和LogDet是不安全的上界，PairDist是安全的下界，而SIGReg消除了间隙。然后我们证明了一个对应定理：在标准常数噪声编码器模型和成功实施SIGReg（各向同性高斯嵌入）下，间隙消失，目标变为精确的信息瓶颈，惊奇界得以保留，潜在目标成本成为AIF实用价值的精确代理，而VICReg则留下不可约的二阶各向异性项。我们将对应关系扩展到多步预期自由能、集成认知价值和学习策略机制，并识别出当前JEPA世界模型未计算的唯一AIF项：状态认知价值，即未来状态覆盖信号。这些预测在种类上而非程度上不同，并在此作为理论结果陈述，留待后续工作中的实证检验；完整证明见附录A，每个结果的代数核心在Lean 4中进行了机器验证（附录D）。

## Abstract
Joint-Embedding Predictive Architectures (JEPAs) are the dominant design for latent world models, yet they are usually justified by empirical performance rather than a normative principle. We show that the choice of anti-collapse regulariser determines whether a JEPA's training objective, a prediction loss plus a weighted embedding regulariser, is a valid Active Inference (AIF) variational free energy. We organise four non-contrastive regularisers (VICReg, LogDet, PairDist, and SIGReg) into an entropy-estimator hierarchy indexed by a prior-miscalibration gap, and show that the gap's sign, whether the estimator bounds the latent entropy from above or below, decides whether the AIF surprise bound survives: VICReg and LogDet are unsafe upper bounds, PairDist a safe lower bound, and SIGReg eliminates the gap. We then prove a correspondence theorem: under the standard constant-noise encoder model and successful SIGReg enforcement (isotropic-Gaussian embeddings), the gap vanishes, the objective becomes an exact information bottleneck, the surprise bound is preserved, and the latent goal cost becomes an exact proxy for AIF pragmatic value, whereas VICReg leaves an irreducible second-order anisotropy term. We extend the correspondence to multi-step expected free energy, ensemble epistemic value, and a learned-policy regime, and we identify the one AIF term no current JEPA world model computes: the state-epistemic value, a future-state coverage signal. The predictions differ in kind, not degree, and are stated here as theoretical consequences left for empirical test in separate work; full proofs are in Appendix A, and the algebraic core of every result is machine-verified in Lean 4 (Appendix D).

---

## 论文详细总结（自动生成）

好的，请看我为您生成的详细中文总结。

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：联合嵌入预测架构（JEPA）是目前学习潜在世界模型的主流方法，但其训练目标的合理性通常仅由经验性能来支撑，缺乏一个统一、规范的**先验原理**。JEPA的训练目标通常包含一个预测损失和一个防止表示坍塌的**反坍塌正则化器**。论文旨在探究：这个目标函数是否可以从 **主动推断（Active Inference, AIF）** 这一规范性框架下的**变分自由能**理论中得到解释。
-   **研究动机**：AIF是一个规范性的智能体理论框架，它将智能行为建模为最小化感知和行动中的变分自由能。JEPA和AIF在许多方面有共同的数学结构，但二者的联系缺乏严格的形式化证明。特别是，JEPA中的反坍塌正则化器（用于提升表示的丰富性）与AIF中的**信息性（informativeness）** 项（用于降低不确定性）之间，其对应关系尚不明确。
-   **整体含义**：本文首次从理论上揭示了，JEPA训练目标的**有效性与规范性根源于其选择的反坍塌正则化器**。具体来说，**SIGReg** 正则化器是唯一能使JEPA目标函数成为**精确**的AIF变分自由能的方法，从而为JEPA世界模型提供了一个坚实的第一性原理基础，并指出了当前模型在理论上缺失的关键部分。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

-   **核心思想**：将反坍塌正则化器视为潜在表示熵（`h(Z)`）的估计器。AIF变分自由能中的信息性项本质上是最大化潜在表示的熵。不同正则化器对熵的估计方式不同，产生了**先验校准误差间隙（prior-miscalibration gap）**。这个间隙的**符号（是上界还是下界）** 决定了JEPA目标是否仍然是有效的AIF自由能（即，能保留对惊奇（surprise）的上界）。
-   **关键技术细节**：
    1.  **熵估计器层次结构**：将四种非对比正则化器按熵估计的准确性分为四个层级：
        -   **Level 0: VICReg**：使用对角协方差矩阵近似，产生一个**松散的上界**（不安全）。最大化它可能不会真正增加真实熵。
        -   **Level 1: LogDet**：使用完整协方差矩阵，产生一个**紧的上界**（不安全）。
        -   **Level 2: PairDist**：非参数方法，给出一个**安全的下界**。
        -   **Level 3: SIGReg**：直接强制嵌入分布为**各向同性高斯分布**，精确地知道熵的值，**消除了所有间隙**（既安全又精确）。
    2.  **定理1（SIGReg-AIF对应定理）**：在以下假设下：
        -   常数噪声编码器模型（`Z = f_phi(X) + epsilon`）。
        -   成功实施SIGReg（嵌入分布 `p_Z` 为各向同性高斯分布）。
        -   定理证明，`SIGReg`使得：
            -   **间隙为零**：`h(Z)` 被精确确定。
            -   **精确信息瓶颈**：JEPA目标等价于AIF变分自由能。
            -   **保留AIF惊奇界**：`F >= -ln p(x)` 成立。
    3.  **对应关系（Dictionary）**：论文清晰地将AIF中的每个元素映射到JEPA世界模型中的对应部分。例如：
        -   AIF复杂度项 `D_KL` → JEPA预测损失MSE。
        -   AIF信息性项 `I(Z; X)` → SIGReg正则化器的值。
        -   AIF实用价值 → 潜在目标成本 `||z_t - z_g||^2`。
    4.  **多步扩展**：将对应关系扩展到多步规划，证明了在标准模型预测控制（MPC）设置下，误差可控，SIGReg仍然保持精确。
    5.  **关键发现：缺失项**：论文识别出AIF期望自由能中的一个关键项——**状态认知价值（state-epistemic value）**，即未来状态的覆盖信号 `h(Z_tau | pi)`，在当前所有JEPA世界模型中**均未被计算**。

-   **公式/算法流程（文字描述）**：
    -   **SIGReg目标函数**：`L = ||z_hat - z||^2 + lambda * SIG-Proxy`。论文证明，这个目标可以精确地重写为AIF变分自由能的形式（即复杂度项减去信息性项）。
    -   **AIF变分自由能**：`F = D_KL[q||p] - E[log p(x|s)]`。通过特定假设（常数噪声、高斯桥接），复杂度项与预测MSE成比例，信息性项与嵌入熵相关。
    -   **预期自由能（EFE）**：论文推导出JEPA世界模型规划成本的EFE形式，并指出其等价于AIF的EFE减去状态认知价值项。

#### 3. 实验设计

-   **数据集 / 场景**：论文明确声明，这是一篇**纯理论**工作。所有关于SIGReg和VICReg之间定性差异的预测（如嵌入条件数、规划排序保真度等）都留待未来的**独立实证研究**验证。
-   **Benchmark**：不适用，因为没有进行实验。
-   **对比方法**：理论分析对比了四种非对比正则化器（VICReg, LogDet, PairDist, SIGReg），但并非实验对比。

#### 4. 资源与算力

-   **未明确提及**。由于是纯理论论文，论文没有提及任何关于GPU型号、数量或训练时长的信息。

#### 5. 实验数量与充分性

-   **无实验**。论文的全部贡献在于理论证明，包括数学推导和形式化验证（使用Lean 4定理证明器，进行了37个定理/引理声明验证，零*sorry*留白）。因此，该论文不存在从传统意义（数据集、消融等）上可直接评价的实验数量与充分性问题。
-   **理论验证充分性**：其核心代数结果经过了形式化证明，逻辑上是自洽且充分的。

#### 6. 论文的主要结论与发现

1.  **反坍塌正则化器的核心作用**：正则化器的选择决定了JEPA世界模型目标函数是否能成为一个有效的AIF变分自由能。这个结论首次将正则化器的选择提升到规范性原理的高度。
2.  **SIGReg的优越性**：SIGReg是唯一一个能同时实现“安全”（保留AIF惊奇界）和“精确”（消除熵估计间隙）的非对比正则化器。相比之下，VICReg和LogDet会导致不可约的各向异性误差项。
3.  **精确映射与缺失项**：成功揭示了JEPA世界模型与AIF之间的精确对应字典。更重要的是，**识别出状态认知价值**（`h(Z_tau | pi)`）是AIF框架中不可或缺但当前所有JEPA世界模型都缺失的关键组件。这为未来JEPA模型的改进指明了方向。
4.  **可证伪的预测**：理论推导给出了SIGReg和VICReg之间在**性质上**而非程度上不同的预测（例如，条件数 `kappa ≈ 1` vs `kappa >> 1`）。这些预测可以直接通过实验进行验证或翻。

#### 7. 优点

-   **理论深度与精确性**：将AIF理论的精神（信息驱动的行为）与JEPA架构（高效学习表示）首次以精确的数学形式连接起来。这比之前纯粹的启发式或经验性结合要深刻得多。
-   **规范性**：为JEPA世界模型的设计提供了“为什么这样做”的规范指导，而不仅仅是“这样做有效”的经验总结。它还提供了一种选择正则化器的标准（保留惊奇界）。
-   **卓越的可证伪性**：传统AIF理论常被批评为不可证伪。本文通过将抽象的AIF项（如变分自由能）与具体、可测量的量（如协方差条件数）联系起来，生成了可以在系统中直接测试的精确预测，使其成为一个可操作的科学框架。
-   **形式化验证**：核心结果使用Lean 4定理证明器进行了机器验证，极大地增强了理论的可信度，避免了纯手动推导的潜在错误。

#### 8. 不足与局限

-   **完全理论性，缺乏实证**：论文最直接的局限是它完全没有提供实验。所有关于定性差异的预测虽然有趣，但还未被验证。文章本身也承认这一点，并将其视为未来工作。
-   **依赖理想化假设**：
    -   **常数噪声编码器**：假设一个固定的、各向同性的观测噪声，这只是一个数学上的解释工具，与实际部署的JEPA系统中使用的确定性编码器不同。
    -   **完美实施**：假设AIF对应关系成立的条件（各向同性高斯分布）得到完美满足。在现实世界中，这只能近似实现。
-   **状态认知价值项的实现难题**：论文仅仅指出“状态认知价值”是缺失项，但并没有提出一个切实可行、高效的去计算它（而不是使用近似的策略集成）的方法。这将计算 `h(Z_tau | pi)` 的难题留给了未来的研究人员。
-   **过完备表示的局限性**：证明所使用的线性可识别性前提，只在匹配维度场景下得到证明，而现实系统通常使用维度远大于潜在自由度数量的编码器，因此该理论的对应成果是否能直接应用还需进一步探讨。

好的，以下是从上次中断处（第8点“不足与局限”的末尾）开始补全的内容，不重复已输出的部分：

---

#### 9. 总结与展望（替代原断点后的缺失部分）

- **核心贡献**：本文首次从**主动推断（AIF）** 的规范性视角，为联合嵌入预测架构（JEPA）世界模型的训练目标提供了严格的理论基础。核心发现是，反坍塌正则化器**SIGReg**是唯一能使JEPA目标函数精确等价于AIF变分自由能的方法，从而确保了训练过程能持续保留对惊奇（surprise）的有效上界，并赋予了模型信息驱动的行为准则。
- **理论价值**：该工作将JEPA的设计从经验驱动的工程实践提升到了**第一性原理**层面。它揭示了现有JEPA模型中缺失的一个关键组件——**状态认知价值**（state-epistemic value），并为未来世界模型的设计提供了明确的改进方向：必须显式建模未来状态的熵，以完整实现AIF框架下的规划。
- **未来方向**：论文明确将**实证验证**列为下一步关键工作。具体包括：
    - 在标准视觉和机器人控制基准上，对比使用SIGReg与VICReg/LogDet等正则化器的JEPA世界模型在长期规划、样本效率及泛化能力上的差异。
    - 验证理论预测的定性差异：SIGReg是否确实能保持嵌入协方差矩阵的条件数接近1，而VICReg会导致条件数显著偏离1。
    - 研究如何高效、近似地计算状态认知价值项（例如通过低维抽象或信息瓶颈方法），并将其集成到现有JEPA模型（如V-JEPA、I-JEPA）中，观察性能提升。
    - 探索在非各向同性假设下（如更灵活的分布族）如何保持AIF对应关系的近似性。
- **总体评价**：本文是一篇**高度原创且理论严谨**的工作。它成功弥合了机器学习中的JEPA框架与理论神经科学中的AIF框架之间的鸿沟，为世界模型的研究提供了坚固的理论锚点。尽管缺乏实证，但其形式化证明和可证伪预测使其成为该领域未来发展的重要基石。

（完）
