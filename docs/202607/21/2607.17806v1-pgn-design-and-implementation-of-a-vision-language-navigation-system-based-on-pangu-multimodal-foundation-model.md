---
title: "PGN: Design and Implementation of a Vision-Language Navigation System Based on Pangu Multimodal Foundation Model"
title_zh: PGN：基于盘古多模态基础模型的视觉语言导航系统设计与实现
authors: "Li Xian, Mingxi Li, Yizheng Wang, Yiming Shen, Qi Chen, Zhuoling Xiao"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.17806v1"
tags: ["query:latent-vla"]
score: 8.0
evidence: 使用基于盘古模型的VLA动作预测的视觉语言导航系统
tldr: "视觉语言导航要求智能体依据自然语言指令和时序视觉观测预测动作序列。本文提出PGN系统，基于OpenPangu-7B盘古多模态模型，分两阶段训练：第一阶段冻结视觉编码器和语言骨干，通过Q-Former和两层MLP投影器实现视觉语言对齐；第二阶段冻结对齐后的视觉通路，采用五观测窗、时间依赖采样及推理-动作输出格式，更新三个结构token嵌入和LoRA适配器以适配导航轨迹。在混合精度和DeepSpeed ZeRO-2优化下，于8块Ascend 910B NPU训练。开环评测500条专家轨迹，归一化动作匹配率62.29%，非空率100%。该工作验证了离线动作预测能力，为闭环导航性能评估提供基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17806v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1169, \"height\": 845, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 804, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1812, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 819, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 854, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17806v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 796, \"height\": 667, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17806v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 280, \"label\": \"Table\"}]"
motivation: 将多模态大模型适配到视觉语言导航任务，需解决视觉语言对齐、紧凑时序输入和稳定训练等关键挑战。
method: 两阶段训练：第一阶段用Q-Former和MLP对齐冻结的EVA-ViT-G/14与OpenPangu-7B；第二阶段冻结视觉通路，引入LoRA和结构token适配导航轨迹。
result: "在500条专家轨迹上开环评测，归一化动作匹配率（NAM）62.29%，非空率（NER）100%。"
conclusion: 验证了离线专家动作预测的有效性，为后续闭环导航及误差累积评估奠定基础。
---

## 摘要
视觉语言导航（VLN）要求具身智能体解释自然语言指令并从时序视觉观测中预测动作。将多模态大语言模型适配到VLN需要视觉-语言对齐、紧凑的时间输入、动作空间接地以及在目标硬件上的稳定训练。本技术报告介绍了PGN（盘古导航器），一个基于OpenPangu-7B构建的离线VLN动作预测系统。训练分为两个阶段。首先，PGMM通过训练Q-Former和两层MLP投影器，将冻结的EVA-ViT-G/14视觉编码器与冻结的语言主干对齐。其次，PGN使用五个观测窗口、依赖于epoch的时间采样以及推理-动作输出格式，将对齐后的模型适配到专家导航轨迹；此阶段冻结对齐后的视觉通路，并更新三个结构标记嵌入和LoRA适配器。该实现在八块Ascend 910B NPU上结合了混合精度计算、选择性FP32计算和DeepSpeed ZeRO-2。在500条留出的专家轨迹上进行教师强制开环评估，V9报告了62.29%的归一化动作匹配（NAM）和100.00%的非空率（NER）。这些指标量化了离线专家动作对齐，而非闭环导航成功；评估误差累积、路径效率和目标完成度仍是未来工作。

## Abstract
Vision-Language Navigation (VLN) requires an embodied agent to interpret a natural-language instruction and predict actions from temporally ordered visual observations. Adapting a multimodal large language model to VLN requires visual-language alignment, compact temporal inputs, action-space grounding, and stable training on the target hardware. This technical report presents PGN (Pangu Navigator), an offline VLN action-prediction system built on OpenPangu-7B. Training proceeds in two stages. First, PGMM aligns a frozen EVA-ViT-G/14 vision encoder with the frozen language backbone by training a Q-Former and a two-layer MLP projector. Second, PGN adapts the aligned model to expert navigation trajectories using five-observation windows, epoch-dependent temporal sampling, and a reasoning-then-action output format; this stage freezes the aligned visual pathway and updates three structural-token embeddings and LoRA adapters. The implementation combines mixed-precision computation, selective FP32 computation, and DeepSpeed ZeRO-2 on eight Ascend 910B NPUs. Under teacher-forced, open-loop evaluation on 500 held-out expert trajectories, V9 reports a 62.29% Normalized Action Match (NAM) and a 100.00% Non-empty Rate (NER). These metrics quantify offline expert-action alignment rather than closed-loop navigation success; evaluating error accumulation, path efficiency, and goal completion remains future work.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：视觉语言导航（VLN）要求具身智能体根据自然语言指令（如“走过厨房，在花瓶旁停下”）和时序视觉观测预测动作序列。将多模态大语言模型（MLLMs）适配到VLN面临四大挑战：
  - 视觉特征与语言模型的**跨模态对齐**；
  - **紧凑时序输入**表示（有限上下文窗口需承载历史观测）；
  - 生成输出需**接地到有限的动作空间**（如前进、转向、停止）；
  - 在特定硬件（如Ascend NPU）上实现**稳定高效的训练**。
- **整体含义**：本文提出PGN（Pangu Navigator），基于OpenPangu-7B构建离线VLN动作预测系统，验证将通用MLLM适配至连续环境导航的可行性，为后续闭环导航评估奠定基础。

## 2. 方法论
- **核心思想**：采用两阶段训练策略，先对齐视觉与语言表征，再通过参数高效适配（LoRA）微调导航任务，避免全模型微调。
- **关键技术细节**：
  - **第一阶段（PGMM）**：冻结EVA-ViT-G/14视觉编码器和OpenPangu-7B语言主干，训练**Q-Former**（32个可学习查询令牌）和**两层MLP投影器**（将768维视觉特征映射至4096维语言嵌入空间）。
  - **第二阶段（PGN）**：冻结对齐后的视觉通路，在专家导航轨迹上训练，引入：
    - **五观测窗口**：当前观测 + 4个历史观测，时序采样步长s可随epoch变化；
    - **epoch-dependent temporal sampling**：起始索引随epoch循环，步长s分阶段变化（如s=4,6,8）；
    - **action-aware sampling**：以概率p替换过采样前进行为为转向/停止；
    - **推理-动作输出格式**：先生成推理文本，再输出规范动作令牌（forward/left/right/stop）；
    - 仅更新**三个结构令牌嵌入**（图像起始、图像结束、帧边界）和**LoRA适配器**。
- **公式/算法流程（文字说明）**：
  - 时间步t的观测窗口：\( O_t = (o_{t-4s}, o_{t-3s}, o_{t-2s}, o_{t-s}, o_t) \)
  - 动作空间：\( A = \{ forward, left, right, stop \} \)
  - 生成过程：\( P(r_t, a_t | I, O_t) = P(r_t | I, O_t) P(a_t | r_t, I, O_t) \)
  - 训练流程：两阶段冻结/更新划分如图3所示。

## 3. 实验设计
- **数据集/场景**：
  - 第一阶段：Microsoft COCO + Visual Genome（图像–描述对）。
  - 第二阶段：自采集的**专家导航轨迹**（通过DAgger兼容管道，β=1即纯专家策略），共19,699条轨迹，1,567,487帧观测。
  - 轨迹：在仿真环境中收集，每个步骤记录专家动作（前进0.25m，转向15°）。
- **Benchmark**：无公开基准，论文自行构建500条留出专家轨迹作为验证集。
- **对比方法**：**未与其他VLN方法对比**（文档明确声明：“不应解释为与已发表VLN系统的比较”），仅在自身版本间纵向比较（V3、V5、V6、V8、V9）。
- **评估指标**：
  - **Exact-string Match (EM)**：生成响应与规范动作字符串完全一致（V8/V9因包含推理文本导致EM=0%）。
  - **Normalized Action Match (NAM)**：提取动作后与专家动作比较（归一化后匹配率）。
  - **Non-empty Rate (NER)**：动作预测非空的比例。

## 4. 资源与算力
- **硬件**：8块 **Ascend 910B NPU**（华为昇腾AI处理器）。
- **训练配置**：
  - 混合精度计算（FP16），但FP16注意力掩码溢出部分**选择性切换至FP32**。
  - 分布式策略：**DeepSpeed ZeRO-2**。
- **训练时长**：**未明确说明**（论文未报告总训练步数或耗时）。

## 5. 实验数量与充分性
- **实验数量**：报告了5个版本（V3、V5、V6、V8、V9）在500条轨迹上的开环评估指标，以及各版本在10个NAM区间内的轨迹分布。
- **消融实验**：**未进行**。论文明确指出，推理-动作输出、身份提示、时间采样、动作感知采样等改动在同一开发周期内引入，无法隔离各组件贡献。因此实验虽多，但**不能归因于单个技术**。
- **客观性与公平性**：
  - 评估为**教师强制开环**（teacher-forced open-loop），即模型输入来自专家轨迹的观测，不执行自身预测的动作，因此不测量误差累积、路径效率或目标完成度。
  - 论文坦承这些指标**不代表闭环导航成功率**，并强调需在仿真环境中执行闭环评估后才能与现有方法公平比较。

## 6. 主要结论与发现
- **核心发现**：V9版本达到 **NAM 62.29%，NER 100.00%**（V8也类似：62.13%, 100.00%），证明OpenPangu-7B经过两阶段训练后可有效进行**离线专家动作对齐**。
- **关键趋势**：从V5到V6，NER从约34%跃升至99%以上（改善了非空生成能力），但NAM基本持平（约23%）；V8/V9通过推理-动作格式大幅提升NAM至62%以上。
- **明确声明**：离线对齐指标不直接等价于导航成功，需后续闭环评估验证泛化性。

## 7. 优点
- **两阶段解耦设计**：视觉-语言对齐和导航任务适配分离，提升了训练稳定性和适配效率。
- **参数高效**：仅训练Q-Former、MLP投影器、3个结构令牌和LoRA，冻结大部分参数，适合7B级模型。
- **结构化输入输出**：五观测窗口+边界令牌+推理文本+规范动作，贴合MLLM的自回归生成。
- **硬件适配**：针对Ascend NPU的FP32选择性计算策略，解决了FP16 Attention溢出问题，保证了训练稳定性。
- **评估透明度**：清晰区分开环动作匹配与闭环导航性能，并指出当前结果仅具开发参考价值。

## 8. 不足与局限
- **评估局限性**：
  - 仅作开环专家动作匹配，**未评估闭环导航**（误差累积、路径效率、目标半径停止等关键指标）。
  - 无消融实验，无法判断各组件（推理输出、采样策略、LoRA等）的独立贡献。
  - 未与现有VLN方法（如基于Transformer的VLN-CE基线）进行对比，**无法定位系统性能水平**。
- **数据局限性**：
  - 专家轨迹由纯专家策略收集（β=1），缺少DAgger迭代训练中的在线交互，可能**偏离实际部署时的分布外问题**。
  - 数据集规模（19k轨迹）相对较小，泛化性存疑。
- **实验设计不足**：
  - 仅500条验证轨迹，统计显著性受限。
  - 未报告置信区间或标准偏差。
  - 未展示训练收敛曲线或超参数敏感性分析。
- **应用限制**：当前系统为离线预测，未接入仿真器执行闭环推理，无法直接用于实际机器人导航。

（完）
